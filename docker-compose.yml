services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ganeshshophub-app:latest
    container_name: ganeshshophub
    restart: always
    ports:
      - "80:8090"
    volumes:
      - pb_data:/app/pb_data

volumes:
  pb_data:
